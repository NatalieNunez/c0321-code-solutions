/* exported omit */
// function omit(source, keys) {
//   for (var key in source) {
//     console.log('source object:', source);
//     console.log('keys array:', keys);
//     console.log('key:', key);
//     if (keys.includes(key)) {
//       console.log('keys includes this key');
//       delete this.key;
//       console.log(source);
//     } else {
//       console.log('keys doesnt include this key');
//     }
//   }
//   return source;
// }

// check each property in source
// if keys includes that property, delete the key value pair
// return the updated object

// issue with deleting key from source
